{
  "name": "ha-voip-card",
  "version": "1.1.0",
  "description": "Home Assistant VoIP Lovelace Card — SIP calling UI with WebRTC",
  "private": true,
  "type": "module",
  "main": "build/ha-voip-card.js",
  "scripts": {
    "build": "rollup -c rollup.config.mjs",
    "watch": "rollup -c rollup.config.mjs --watch",
    "clean": "rimraf build",
    "prebuild": "rimraf build"
  },
  "dependencies": {
    "lit": "^3.1.2",
    "@lit/reactive-element": "^2.0.4",
    "tslib": "^2.6.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "rollup": "^4.9.6",
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@rollup/plugin-typescript": "^11.1.6",
    "rollup-plugin-terser": "^7.0.2",
    "@rollup/plugin-json": "^6.1.0",
    "rimraf": "^5.0.5"
  }
}
